- provide(:title, "Edit")
%h1 Update your profile
.row
  .span6.offset3
    = form_for(@user) do |f|
      = render 'shared/error_messages', object: f.object
      = render 'fields', f: f
      = f.label :avatar, "New photo?"
      = f.file_field :avatar, class: "btn btn-mini btn-inverse disabled"
      = f.submit "Save!", :disable_with => "Saving", class: "btn btn-large btn-inverse pull-right"
    = gravatar_for @user
    %a{:href => "http://gravatar.com/emails", :target => "_blank"} gravatar
